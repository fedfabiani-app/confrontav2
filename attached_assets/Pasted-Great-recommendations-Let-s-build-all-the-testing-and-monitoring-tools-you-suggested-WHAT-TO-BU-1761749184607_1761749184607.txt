Great recommendations! Let's build all the testing and monitoring tools you suggested.

WHAT TO BUILD:

1. MANUAL TRIGGER API ENDPOINTS
Location: server/routes.ts (alongside existing /api/refresh endpoints)

Endpoints needed:
a) POST /api/refresh-weekly/all
   - Trigger full week scrape (all sources)
   - Auth: X-Admin-Secret header (reuse existing ADMIN_SECRET pattern)
   - Body: { targetWeek?: string, forceRescrape?: boolean, dryRun?: boolean }
   - dryRun mode: Run skip logic, return what WOULD be scraped, DON'T enqueue jobs

b) POST /api/refresh-weekly/source/:sourceId
   - Trigger single source (all 12 signs)
   - Same auth and dryRun support
   - Useful for testing specific sources

c) POST /api/refresh-weekly/sign/:sign
   - Trigger single sign (all sources)
   - Same pattern as daily /api/refresh/sign/:sign

Validation for all endpoints:
- Verify targetWeek is a valid Monday (use weekUtils)
- Verify sourceId exists in weekly_sources table
- Verify sign is valid zodiac sign
- Prevent simultaneous runs (check hasRunningWeeklyExecution)

Response format:
{
  success: true,
  dryRun: false,
  executionId: 123,
  stats: {
    sourcesTargeted: 8,
    signsProcessed: 12,
    jobsEnqueued: 96,
    jobsSkipped: 0
  },
  message: "Weekly scrape triggered for week of Oct 27-Nov 2, 2025"
}

2. MONITORING & TESTING ENDPOINTS
Add these admin endpoints:

GET /api/admin/weekly-scraper/guards
- Test all guard functions without triggering scrapes
- Returns current state of all guards (as you suggested in the test endpoint example)
- Response: { hasRunning, mondayDone, elleGroupDone, saturdayGroupDone, isMonday, isThursday, isSaturday }

GET /api/admin/weekly-scraper/coverage
- Check current week coverage
- Returns which sources have data, which are missing
- Use your "Coverage Check" SQL query

GET /api/admin/weekly-scraper/executions
- Recent execution history
- Use your "Execution History" SQL query
- Optional query params: limit (default 10), sourceGroup filter

GET /api/admin/weekly-scraper/health
- Health check endpoint with warnings
- Check for:
  * No completed execution this week (by Tuesday)
  * Stale running executions (>2 hours old)
  * Coverage below 95%
  * Failed sources that need attention
- Return: { healthy: boolean, warnings: string[], stats: {...} }

3. STALE EXECUTION CLEANUP
You mentioned this concern - add to the orchestrator:

Before starting a new execution, check for stale ones:
- Find executions with status='running' AND started_at > 2 hours ago
- Update them to status='stale' or 'timeout'
- Log warning
- Proceed with new execution

Add this check to hasRunningWeeklyExecution() helper.

4. DRY RUN MODE
Modify weeklyScraperOrchestrator.ts to support dryRun:

When dryRun=true:
- Go through ALL the logic (load sources, check skip logic, etc.)
- Log what WOULD be enqueued
- Return stats about what would happen
- DON'T actually call enqueueWeeklyScrapeJob()
- Mark execution with a 'dry_run' flag in database

This is critical for testing without hitting actual sources!

5. SQL VERIFICATION QUERIES
Create a new file: server/utils/weeklyScraperQueries.ts

Export these functions (using your SQL recommendations):
- getWeeklyCoverage(weekStart: Date) - Coverage check query
- getExecutionHistory(limit: number, sourceGroup?: string) - History query
- getUpsertVerification(weekStart: Date) - Check which sources were replaced
- findGapsInCoverage(weekStart: Date) - Missing data query
- getFailedSources(weekStart: Date) - Sources that need retry

These will be used by the monitoring endpoints.

6. FALLBACK/RETRY LOGIC
You pointed out weekly doesn't have automatic retry like daily does. Add:

Optional 4th cron job: Weekly Fallback Retry
- Schedule: Monday 9:00 AM (after main window)
- Check for failed sources from Monday run
- Retry only those sources
- Same pattern as daily fallback

Or just rely on manual trigger? Give me your recommendation and implement accordingly.

REQUIREMENTS:
- All endpoints require admin auth (X-Admin-Secret)
- Comprehensive error handling
- Match the logging/response style of existing endpoints
- TypeScript types for all request/response objects
- Add JSDoc comments

Build all of these for me:
1. The 3 manual trigger endpoints
2. The 4 monitoring endpoints  
3. The stale execution cleanup logic
4. The dryRun mode in orchestrator
5. The SQL query utility functions
6. Your recommendation on fallback retry (and implementation if you think it's needed)

Show me how they all integrate together.