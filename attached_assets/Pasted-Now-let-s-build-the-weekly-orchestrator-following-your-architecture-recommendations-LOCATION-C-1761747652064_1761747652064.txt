Now let's build the weekly orchestrator following your architecture recommendations.

LOCATION:
Create: server/services/weeklyScraperOrchestrator.ts (mirrors the daily one)

YOUR KEY INSIGHTS I'M INCORPORATING:
1. Use source_group tracking: 'all', 'elle_only', 'saturday_group'
2. Monday checks week-level completion (not source-level)
3. Thursday/Saturday check source-group-level completion
4. Use forceRescrape: true for replacement scrapes
5. UPSERT happens automatically via unique constraint (source_id, sign_id, week_start)

ORCHESTRATOR FUNCTION:
Main function: runWeeklyScraperCycle(options)

Options should accept:
- weekStart: Date (Monday of target week, defaults to current)
- specificSources: number[] (optional, for fallback or filtered scrapes)
- sourceGroup: 'all' | 'elle_only' | 'saturday_group' (for tracking)
- forceRescrape: boolean (bypass skip logic for replacement scrapes)
- triggerType: 'scheduled' | 'manual' | 'fallback'

ORCHESTRATOR FLOW:
1. Create execution record in weekly_scraper_executions
   - Include source_group so we can track which cycle ran
   
2. Load sources to scrape
   - If specificSources provided: use those
   - Otherwise: load all enabled weekly sources
   
3. Process 12 zodiac signs sequentially (5-second delays)
   - For each sign, for each source:
     * Apply skip logic (see below)
     * If should scrape: enqueue job via enqueueWeeklyScrapeJob()
   
4. Update execution record with stats

SKIP LOGIC (Critical Part):
You explained this well - implement it as:

- If forceRescrape is true: DON'T skip anything (Thu/Sat replacement scrapes)
- Otherwise:
  * Check weekly_scraper_executions: Has this source_group completed for this week?
  * Check weekly_scraper_source_status: Has this specific source+sign been scraped?
  * Check weekly_horoscope_data: Does data exist for this source+sign+week?
  * Only scrape if checks fail (no data exists)

The key difference you mentioned:
- Monday: Check if ANY Monday scrape completed this week (week-level)
- Thursday: Check if elle_only group completed this week (source-group-level)
- Saturday: Check if saturday_group completed this week (source-group-level)

HELPER FUNCTIONS:
Build these helpers inside the file:
- hasCompletedGroupScrapeForWeek(weekStart, sourceGroup): Check execution table
- getFailedWeeklySources(weekStart, sourceGroup?): For fallback retry

PATTERN TO FOLLOW:
Mirror my dailyScraperOrchestrator.ts structure:
- Same logging style
- Same error handling (try-catch, update execution status)
- Same sequential processing with delays
- Same job enqueueing pattern

Use the week utilities we just built (getCurrentWeekStart, getMondayOfWeek, etc.)

Build the complete orchestrator for me with comprehensive logging so I can see what's happening at each step.